<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local ="clr-namespace:XamarinToDoApp.ViewModels"
             x:Class="XamarinToDoApp.MainPage">

    <ContentPage.BindingContext>

        <local:ToDoListViewModel/>

    </ContentPage.BindingContext>

    <StackLayout>
        <Entry 
        Text="{Binding NewToDoTextValue}" 
        Placeholder="Enter a todo"
        ReturnCommand="{Binding AddNewToDoCommand}"
        />
        <ListView ItemsSource="{Binding ToDoItems}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                            <Label Text="{Binding Text}" FontSize="30" Padding="1"></Label>
                    </ViewCell>
                </DataTemplate>

            </ListView.ItemTemplate>

        </ListView>
    </StackLayout>

</ContentPage>
